# Contracts package

```bash
# compile + unit tests
pnpm test

# local node
pnpm node

# deploy (another terminal)
pnpm deploy:local
# or
pnpm deploy:sepolia
```

Deployed addresses should be copied into `apps/dapp/.env`.
